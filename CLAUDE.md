# LyricStage project guide

Read `AGENTS.md`, `README.md`, and the relevant ADR before changing architecture. This repository owns the Web Performance Runtime, browser extension, providers, contracts, rendering packages, performance lab, and optional Director gateway. BiliMusic is an external iOS contract consumer, not the implementation owner.
