# Xcode / 构建产物(工程由 xcodegen 从 project.yml 生成,不入库)
BiliMusic.xcodeproj/
DerivedData/
build/
*.xcuserstate
xcuserdata/

# macOS
.DS_Store

# 测试下载的音频
scripts/*.m4a

# 本地签名配置(各自的 Team ID,不入库;模板见 Local.xcconfig.example)
Local.xcconfig

# Claude Code 本地代码索引缓存(机器相关,可再生)
.claude/index.json
